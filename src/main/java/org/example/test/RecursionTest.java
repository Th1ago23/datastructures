package org.example.test;

import org.example.exercises.Recursion;

public class RecursionTest {
    public static void main(String[] args) {
        Recursion rec = new Recursion(20);
        rec.returnUntilN(20, 1);
    }
}
